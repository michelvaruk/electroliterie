{% for item in items %}
    <div class="filter-checkbox">
        <input type="checkbox" id="{{ item }}" name="{{ item }}" data-feature="{{ feature }}" data-action="click->filter#check" data-filter-target="filters" checked />
        <label for="{{ item }}">{{ item }}</label>
    </div>
{% endfor %}