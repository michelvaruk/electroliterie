<form method="post" action="{{ path('app_edit_profession_delete', {'id': profession.id}) }}" onsubmit="return confirm('Attention, vous êtes sur le point de supprimer ce métier, en êtes-vous certain ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profession.id) }}">
	<button class="btn {{ className|default }}">Supprimer</button>
</form>