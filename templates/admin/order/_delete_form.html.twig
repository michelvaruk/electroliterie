<form method="post" action="{{ path('app_edit_order_delete', {'id': order.id}) }}" onsubmit="return confirm('Attention, vous êtes sur le point de supprimer cette commande, en êtes-vous certain ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.id) }}">
	<button class="btn {{ className|default }}">Supprimer</button>
</form>