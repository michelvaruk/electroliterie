{% extends 'login-base.html.twig' %}

{% block title %}Génération du mot de passe{% endblock %}

{% block body %}
    <h1>Génération du mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="w-full text-white bg-blue-600 mt-4 hover:bg-blue-700 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Enregistrer</button>
    {{ form_end(resetForm) }}
{% endblock %}
